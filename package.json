{
  "name": "@f/filter",
  "description": "Filter for various data types.",
  "repository": "git://github.com/micro-js/filter.git",
  "version": "1.1.1",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/filter-array": "^1.0.0",
    "@f/filter-obj": "^1.0.0",
    "@f/is-array": "^1.0.0",
    "@f/is-object": "^1.0.0"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
